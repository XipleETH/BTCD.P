{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd packages/frontend && npm ci && npm run build && cd ../../ && rm -rf dist && mkdir -p dist && cp -r packages/frontend/dist/* dist/",
  "installCommand": "npm install",
  "framework": "vite",
  "outputDirectory": "dist",
  "functions": {
    "api/**/*": {
      "runtime": "edge"
    }
  }
}
