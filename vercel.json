{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm -w packages/frontend run build && rm -rf dist && mkdir -p dist && cp -r packages/frontend/dist/* dist/",
  "installCommand": "npm install",
  "framework": "vite",
  "outputDirectory": "dist",
  "functions": {
    "api/**/*": {
      "runtime": "edge"
    }
  }
}
